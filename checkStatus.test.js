const { hasUncaughtExceptionCaptureCallback } = require('process');
const { checkStatus } = require('./script')

test ('should give true value' , () => {

    const text = checkStatus('true');
      if (newTileID == firstTileID)

 expect(text).toBe('true');
});